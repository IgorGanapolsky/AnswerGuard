appId: com.igorganapolsky.answerguard
---
# CI smoke test: launch AnswerGuard and verify the stable home surface.
- launchApp:
    clearState: true
- waitForAnimationToEnd

# Dismiss common emulator system popups and App Distribution popups if present
- runFlow: dismiss-popups.yaml

- extendedWaitUntil:
    visible: 'Call Screening'
    timeout: 60000

- assertVisible: 'AnswerGuard'
- assertVisible: 'Call Screening'
# Subscription tier card lives in Settings (not Home).
- runFlow: open-settings-subscription.yaml
